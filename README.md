# YumStudio-DebugKernel

YumStudio game engine's debug kernel.
It's better to use YumStudio engine than using the debug kernel directly.

Running an assembly:
```bash
<KernelPath> --asm "path/to/assembly"
```